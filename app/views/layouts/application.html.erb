<!DOCTYPE html>
<html>
<head>
  <link href='http://fonts.googleapis.com/css?family=Lato:400,700' rel='stylesheet' type='text/css'>
  <title>G2Store</title>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<% if is_admin? %>
<nav><%= link_to 'Add Publisher', new_publisher_path %> </nav>
<% else %>
<nav></nav>
<% end %>
<% if session[:id].present? %>
  <p style="float: right;">Welcome, <%= User.find(session[:id]).email %> | <%= link_to 'Logout', logout_path, method: :delete %></p>
<% end %>

<div class="flash_notice">
  <%= flash[:notice] %>
</div>

<div class="errors">
  <%= flash[:alert] %>
</div>

<%= yield %>
</body>
</html>